<h1>Listing time_periods</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Start time</th>
      <th>End time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @time_periods.each do |time_period| %>
      <tr>
        <td><%= time_period.name %></td>
        <td><%= time_period.start_time %></td>
        <td><%= time_period.end_time %></td>
        <td><%= link_to 'Show', time_period %></td>
        <td><%= link_to 'Edit', edit_time_period_path(time_period) %></td>
        <td><%= link_to 'Destroy', time_period, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Time period', new_time_period_path %>
