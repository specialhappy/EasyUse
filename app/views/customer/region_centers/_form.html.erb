<%= form_for(@region_center) do |f| %>
  <% if @region_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@region_center.errors.count, "error") %> prohibited this region_center from being saved:</h2>

      <ul>
      <% @region_center.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
