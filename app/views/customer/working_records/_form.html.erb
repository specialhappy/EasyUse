<%= form_for(@working_record) do |f| %>
  <% if @working_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@working_record.errors.count, "error") %> prohibited this working_record from being saved:</h2>

      <ul>
      <% @working_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
