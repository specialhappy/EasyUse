<%= form_for(@a_level_tag) do |f| %>
  <% if @a_level_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@a_level_tag.errors.count, "error") %> prohibited this a_level_tag from being saved:</h2>

      <ul>
      <% @a_level_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
