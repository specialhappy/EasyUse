<%= form_for [:manager, @manager_a_level_tag] do |f| %>
<% if @manager_a_level_tag.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@manager_a_level_tag.errors.count, "error") %> prohibited this manager_a_level_tag from being saved:</h2>

	<ul>
		<% @manager_a_level_tag.errors.full_messages.each do |message| %>
		<li>
			<%= message %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="form-group field">
	<%= f.label :一级标签名称,:class=>'col-sm-2 control-label' %>
	<div class="col-sm-10">
		<%= f.text_field :name,:class=>'form-control' %>
	</div>
</div>
<div class="form-group field">
	<%= f.label :标签描述,:class=>'col-sm-2 control-label' %>
	<div class="col-sm-10">
		<%= f.text_area :description,:class=>'form-control' %>
	</div>
</div>
<div class="form-group field actions">
	<div class="pull-right">
		<%= f.submit '提交',:class=>"btn btn-default"%>
	</div>
</div>
<% end %>
