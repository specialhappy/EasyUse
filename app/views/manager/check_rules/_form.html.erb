<%= form_for [:manager, @manager_check_rule] do |f| %>
  <% if @manager_check_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manager_check_rule.errors.count, "error") %> prohibited this manager_check_rule from being saved:</h2>

      <ul>
      <% @manager_check_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group field">
	<%= f.label :规则名 ,:class=>'col-sm-2 control-label' %>
	<div class="col-sm-10">
		<%= f.text_field :name,:class=>'form-control'  %>
	</div>
</div>
<div class="form-group field">
	<%= f.label :规则描述,:class=>'col-sm-2 control-label'  %>
	<div class="col-sm-10">
		<%= f.text_area :description,:class=>'form-control'  %>
	</div>
</div>
<!--div class="form-group field">
	<%= f.label :是否启用,:class=>'col-sm-2 control-label'  %>
	<div class="col-sm-10">
		<%= f.check_box :applid ,:class=>'form-control' %>
	</div>
</div-->

 
<div class="form-group field actions">
	<div class="pull-right">

		<%= f.submit '提交',:class=>"btn btn-default"%>
	</div>
</div>
<% end %>
