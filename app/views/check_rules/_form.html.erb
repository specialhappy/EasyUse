<%= form_for(@check_rule) do |f| %>
  <% if @check_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@check_rule.errors.count, "error") %> prohibited this check_rule from being saved:</h2>

      <ul>
      <% @check_rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
